{
  "name": "codificando",
  "version": "1.0.0",
  "description": "codificando projeto de teste",
  "main": "index.js",
  "scripts": {
    "lint": "tslint -p tsconfig.json -c tslint.json",
    "build": "tsc -p ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node -r ./register-paths.js ./build",
    "start:dev": "tsc && ENV_VAR=dev tsnd -r tsconfig-paths/register --respawn --ignore-watch node_modules src/index.ts"
  },
  "author": "Pedro",
  "license": "MIT",
  "dependencies": {
    "luxon": "^3.4.2"
  },
  "devDependencies": {
    "@types/luxon": "^3.4.2",
    "@types/node": "^20.12.7",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "tslint": "^6.1.3",
    "typescript": "^5.4.5"
  }
}
